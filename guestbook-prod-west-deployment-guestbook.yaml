apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
  namespace: guestbook-prod-west
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      labels:
        app: guestbook
    spec:
      containers:
      - image: ghcr.io/wanghong230/guestbook:v1.1.12
        name: guestbook
        ports:
        - containerPort: 3000
          name: http
