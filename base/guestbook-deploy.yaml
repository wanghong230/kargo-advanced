apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook
  template:
    metadata:
      labels:
        app: guestbook
    spec:
      containers:
      - image: ghcr.io/wanghong230/guestbook:latest
        name: guestbook
        env:
        - name: KARGO_DEMO_ENV
          value: "GARTNER"
        ports:
        - name: http
          containerPort: 3000
